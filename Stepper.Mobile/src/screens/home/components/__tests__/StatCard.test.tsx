// StatCard tests have moved to components/common/__tests__/StatCard.test.tsx
// This file is intentionally left minimal to avoid duplicate test runs.

describe('StatCard (home) - MOVED', () => {
  it('tests have been consolidated to components/common/__tests__/StatCard.test.tsx', () => {
    expect(true).toBe(true);
  });
});
